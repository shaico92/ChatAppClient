import React, { useState } from "react";
//import NavItems from "../../Constants/Constants";

const Home = () => {
  const [pageIndex, setPageIndex] = useState(0);

  return <div className={"landing"}>home</div>;
};

export default Home;
